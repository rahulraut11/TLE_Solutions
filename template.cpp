#include <bits/stdc++.h>
using namespace std;

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        
    }
    return 0;
}
